import { IStatusLevel } from '../interfaces/istatus-level';

export const StatusLevel: IStatusLevel = {
  default: 'default',
  error: 'error',
  info: 'info',
  success: 'success',
  warning: 'warning',
};
